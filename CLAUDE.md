# CLAUDE.md

## Project Overview

A vanilla JavaScript todo app with Firebase authentication and Firestore sync, deployable to Netlify. No build system, no npm, no `package.json` — just static files served directly.

## Tech Stack

- **Frontend**: Vanilla JS (ES6 modules), no framework, no bundler
- **Styling**: Vanilla CSS with custom properties for light/dark theming; Inter via Google Fonts
- **Auth/DB**: Firebase v10 from the gstatic CDN (Google Sign-In + Cloud Firestore)
- **Drag-and-drop**: Sortable.js v1.15.0 (jsDelivr CDN)
- **Deployment**: Netlify (build script generates firebase config from env vars)

## Local Development

No install step required. Just serve the directory:

```bash
python -m http.server 8000
# or
npx http-server
```

Then open `http://localhost:8000`. (`.vscode/launch.json` is configured for port 8080.)

**Firebase config setup**: Copy `firebase-config.example.js` to `firebase-config.js` and fill in your credentials. This file is gitignored — never commit it. `index.html` does a `HEAD` fetch for it on load and shows the `#configMissing` banner if absent.

## Project Structure

```
index.html                    # DOM skeleton (~189 lines); all content generated by JS
main.js                       # All app logic (~1660 lines)
firebase.js                   # Firebase auth/Firestore wrapper (~57 lines)
firebase-config.example.js    # Credential template (copy to firebase-config.js)
styles.css                    # All styles including light/dark themes (~1730 lines)
netlify.toml                  # Netlify build config
README.md
scripts/
  generate-firebase-config.js # Generates firebase-config.js from Netlify env vars
.github/
  copilot-instructions.md     # AI coding guidelines for this project
  copilot-instructions.new.md   # stale draft
  copilot-instructions.new2.md  # stale draft
.vscode/launch.json           # Chrome debug config (localhost:8080)
styles.zip                    # stale backup, not used at runtime
```

## Architecture

### State Management
All state lives in `main.js` as module-level variables:
- `tasks[]` — the canonical task array
- `activeUserId`, `currentUser`, `accountMenuOpen` — auth state
- `statusFilter`, `categoryFilter`, `sortMode` — view state
- `editingTaskId`, `editingTaskText`, `openTaskMenuId` — UI state
- `tasksUnsubscribe`, `isRemoteUpdate`, `lastSyncAt`, `lastSyncError`, `listenerActive` — sync state
- `renderedTaskIds`, `lastAddedSubtaskId`, `lastAddedSubtaskTaskId` — animation bookkeeping

### Task Data Model
```javascript
{
  id: string,          // Date.now().toString()
  text: string,
  done: boolean,       // legacy mirror of status === "done"
  status: "todo" | "doing" | "done",
  category: string,    // "General" | "Home" | "Work" | "Personal"
  dueDate: string,     // YYYY-MM-DD, or ""
  isDaily: boolean,
  subtasks: [{ id, text, done, status }]
}
```

### Persistence
- **Guest users**: `localStorage` key `todo_tasks_guest`
- **Signed-in users**: Firestore doc `users/{uid}/taskLists/default` (whole task array + `updatedAt` server timestamp), written with `setDoc({ merge: true })`
- On first sign-in, guest data migrates to Firestore automatically, guarded by the `todo_migrated_{uid}` localStorage flag

### Render Pattern
Every state change calls `save()` → persists to storage → calls `render()`. Full DOM teardown and rebuild on each change (acceptable at task-list scale).

### Sync and the `isRemoteUpdate` guard
`startRemoteSync()` (`main.js:147`) subscribes to Firestore via `onSnapshot`. When a snapshot arrives it sets `isRemoteUpdate = true` around the `tasks` assignment and `render()`, then clears it. `save()` (`main.js:127`) checks this flag and skips the Firestore write when set — this is what prevents the write → snapshot → write echo loop. **If you add a code path that mutates `tasks` from remote data, wrap it the same way.**

### Filtering & Sorting
`render()` works on a shallow copy of `tasks`. Sort modes: `manual`, `dueAsc`, `dueDesc`, `newest`, `oldest`, `alpha`, `dailyTop`. Only `manual` persists order back to the `tasks` array (via the Sortable `onEnd` handler, `main.js:1300`); all other modes are view-only.

## Key Functions (main.js)

| Area | Function | Line |
|---|---|---|
| Data migration | `normalizeTasks()` | 19 |
| Persistence | `save()` / `loadTasks()` | 127 / 141 |
| Sync | `startRemoteSync()` / `stopRemoteSync()` / `setSyncStatus()` | 147 / 189 / 198 |
| Task CRUD | `addTask()` / `addSubtask()` / `deleteTask()` / `clearDone()` | 233 / 248 / 361 / 419 |
| Status | `toggleDone()` / `cycleTaskStatus()` / `autoPromoteParentStatus()` | 280 / 291 / 395 |
| Undo | `showUndoToast()` | 548 |
| Date picker | `openDueDatePicker()` | 581 |
| Drawers | `renderDaily()` / `updateWorkingDrawer()` | 716 / 794 |
| Main render | `render()` | 863 |
| Drag-and-drop | `initSortable()` / `updateSortableState()` | 1291 / 1320 |
| Theme | `applySavedTheme()` | 1328 |
| Auth UI | `initAuthUI()` | 1424 |

`firebase.js` exports: `auth`, `provider`, `signIn`, `signOutUser`, `onAuth`, `saveTaskList`, `onTaskList`.

### Init sequence (end of main.js)
`applySavedTheme()` → `loadTasks()` → `syncOptionsPanelToViewport()` → `render()` → `initSortable()`. `initAuthUI()` runs separately at DOM ready and calls `startRemoteSync()` once Firebase reports a signed-in user.

## Deployment (Netlify)

Set these environment variables in Netlify:
- `FIREBASE_API_KEY`
- `FIREBASE_AUTH_DOMAIN`
- `FIREBASE_PROJECT_ID`
- `FIREBASE_STORAGE_BUCKET`
- `FIREBASE_MESSAGING_SENDER_ID`
- `FIREBASE_APP_ID`
- `FIREBASE_MEASUREMENT_ID` (optional)

The build command (`node scripts/generate-firebase-config.js`) generates `firebase-config.js` from these vars and exits non-zero if a required one is missing. Publish directory is `.`.

## Key Conventions

- **XSS**: use `textContent` (not `innerHTML`) for user-provided text. The render path already does this throughout; the only `innerHTML` uses are static strings, list clearing, and the debug panel (non-user data). Note `escapeHtml()` (`main.js:516`) is defined but currently unused — don't assume it's protecting anything.
- **`normalizeTasks()` is the single migration boundary.** All data entering the system — from localStorage *and* Firestore — passes through it. It maps legacy `done` booleans to `status` strings and backfills missing subtask IDs. Put new schema migrations here.
- **`autoPromoteParentStatus()` only promotes.** A parent becomes `"doing"` if any subtask is `"doing"`; it never demotes, and it skips tasks already marked done. Call it after any subtask status mutation.
- **Undo pattern**: `deleteTask`, `toggleDaily`, and `clearDone` all route through `showUndoToast()`, whose undo closure splices items back at their original index. Only one toast exists at a time. Keep new destructive actions consistent with this.
- Drag-and-drop (Sortable.js) is only enabled in `manual` sort mode; `updateSortableState()` toggles the `disabled` option.
- Theme is persisted in `localStorage` as `"theme"` (`"dark"` or `"light"`), applied as a `dark` class on `<body>`; first load falls back to `prefers-color-scheme`.
- Category colors are duplicated between pills and filter dots in `styles.css` — General `#4b5563`, Home `#f97316`, Work `#2563eb`, Personal `#10b981`. Update both places.
- `styles.css` honors `prefers-reduced-motion` — keep new animations inside that guard.
- **No automated tests.** Verify manually across: task CRUD, subtasks, filters, sorting, drag-and-drop, auth flow (sign in/out + guest migration), daily/working drawers, offline sync status, and dark mode.

## Branch

Active development branch: `claude/add-claude-md-v8oOT`
